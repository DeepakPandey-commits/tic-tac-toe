import type { SymbolKind } from "../types";

/** Keep in sync with the CSS custom properties in src/index.css. */
export const ACCENT_HEX: Record<SymbolKind, string> = {
  X: "#4f9dff",
  O: "#ff6b9d",
};
