# Port the WebSocket/HTTP server listens on.
# Most hosting platforms (Render, Railway, Fly.io, etc.) set this automatically —
# only override it for local development if 8080 is already in use.
PORT=8080
